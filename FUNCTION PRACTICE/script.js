// 1. Create a function that prints "Hello!"
let greet = () =>{
    return "Hello"
}
console.log(greet());

// 2. Create a function that returns your name.
let myname = (name) =>{
    return name;
}
console.log(myname("Laiba"));

// 3. Write a function that adds 2 and 3 (no parameters).
let add = () =>{
    return 2 + 3;
}
console.log(add());

// 4. Write a function that subtracts 10 from 100 (no parameters).
let sub = () =>{
    return 100 - 10;
}
console.log(sub());

// 5. Create a function that returns any number you choose.
let num = (n1) =>{
    return n1;
}
console.log(num(32));
 
// 7. Make a function that takes two numbers and returns their sum.
let sum = (n2,n3) =>{
    return n2 + n3;
}
console.log(sum(4,2));

// 8. Create a function that multiplies 4 by 5 and returns the result.
let multi = () =>{
    return 4 * 5
}
console.log(multi());

// 9. Make a function that divides 20 by 2 and returns the result.
let div = () =>{
    return 20 / 2;
}
console.log(div());

// 10. Create a function that takes one number and adds 10 to it.
let number = (num) =>{
    return num + 10;
}
console.log(number(30));

// 11. Write a function that takes two numbers and subtracts the second from the first.
let subnum = (n4,n5) =>{
    return n4 - n5;
}
console.log(subnum(12,8));

// 12. Make a function that takes two numbers and multiplies them.
let multinum = (n6,n7) =>{
    return n6 * n7;
}
console.log(multinum(5,7));

// 13. Create a function that divides the first number by the second.
let division = (a,b) =>{
    return a / b
}
console.log(division(14,7));

// 14. Write a function that returns true if the number is greater than 10.
let numx = (x) =>{
    if(x > 10){
        return "Greater"
    }
    else{
        return "Smaller"
    }
}
console.log(numx(5));

// 15. Create a function that checks if a number is negative.
let numa = (a) =>{
    if(a < 0){
        return "negative"
    }
    else{
        return "Not negative"
    }
}
console.log(numa(-4));

// 16. Make a function that returns true if a number is equal to 5.
let numb = (b) =>{
    if(b == 5){
        return true;
    }
    else{
        return false;
    }
}
console.log(numb(3));

// 17. Write a function that checks if a number is zero.
let numc = (c) =>{
    if(c == 0){
        return "Its zero"
    }
    else{
        return "Not zero"
    }
}
console.log(numc(8));

// 18. Create a function that returns double of a number.
let numd = (d) =>{
    return d * 2
}
console.log(numd(8));

// 19. Write a function that returns half of a number.
let nume = (e) =>{
    return e / 2;
}
console.log(nume(6));

// 20. Make a function that adds 1 to any number.
let numf = (f) =>{
    return f + 1;
}
console.log(numf(9));

// 21. Create a function that returns true if the input is greater than 100.
let numg = (g) =>{
    if(g > 100){
        return true;
    }
    else{
        return false;
    }
}
console.log(numg(150));

// 22. Write a function that subtracts 7 from any number.
let numh = (h) =>{
    return h - 7;
}
console.log(numh(15));

// 23. Create a function that checks if a number is divisible by 2 (use %).
let numi = (i) =>{
    if(i % 2){
        return "Divisible by 2"
    }
    else{
        return "Not divisible by 2"
    }
}
console.log(numi(7));

// 24. Make a function that returns true if two numbers are equal.
let numk = (k,x) =>{
    if(k == x){
        return "Both numbers are equal"
    }
    else{
        return "Not equal"
    }
}
console.log(numk(9,9));

// 25. Write a function that returns true if the first number is greater.
let numm = (m,x) =>{
    if(m > x){
        return "First number is Greater"
    }
    else{
        "Not greater"
    }
}
console.log(numm(94,87));

// 26. Create a function that takes three numbers and returns their total.
let numn = (a,b,c) =>{
    return a + b + c;
}
console.log(numn(57,87,12));

// 27. Make a function that takes three numbers and returns their average.
let numo = (x,y,z) =>{
    return x + y + z / 3;
}
console.log(numo(9,12,6));

// 28. Write a function that multiplies a number by itself (square).
let nump = (p) =>{
    return p * p;
}
console.log(nump(9));

// 29. Create a function that returns true if a number is between 1 and 10.
let numq = (q) =>{
    if(q > 1 && q < 10){
        return "The number is between 1-10"
    }
    else{
        return "Not between 1-10"
    }
}
console.log(numq(6));

// 30. Write a function that adds 5, then multiplies by 2.
let numr = (r) =>{
    return (r + 5) * 2;
}
console.log(numr(4));

// 31. Create a function that adds 3 numbers together.

// 32. Write a function that subtracts 3 numbers (a - b - c).

// 33. Make a function that takes one number and returns its negative.

// 34. Create a function that checks if a number is odd.

// 35. Write a function that checks if a number is even.

// 36. Make a function that returns the absolute value of a number (if negative, return positive).

// 37. Create a function that returns 1 if the number is positive, 0 if it's 0, -1 if negative.

// 38. Write a function that returns the remainder when divided by 3.

// 39. Create a function that multiplies a number by 10.

// 40. Make a function that adds 100 to any number.

// 41. Write a function that returns true if two values are not equal.

// 42. Create a function that adds 2 to a number and checks if result is greater than 10.

// 43. Make a function that returns the square root of a number using Math.sqrt.

// 44. Write a function that returns the cube of a number.

// 45. Create a function that returns the result of (a + b) * c.

// 46. Write a function that returns true if a number is between 50 and 100.

// 47. Create a function that subtracts 5 from a number and returns whether the result is positive.

// 48. Make a function that returns true if the number is a multiple of 5.

// 49. Write a function that takes two numbers and returns the greater one.

// 50. Create a function that takes two numbers and returns the smaller one.

